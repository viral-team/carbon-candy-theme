// mapped properties for keys in package.json vs keys in themeJSON
const PKG_PROP_MAP = {
	"carbonCandy.hidesExplorerArrows": "hidesExplorerArrows",
	"carbonCandy.folders.associations": "folders.associations",
	"carbonCandy.files.associations": "files.associations",
};

const MESSAGES = {
	needsReloadToSync: "A Reload is needed to sync with the updated settings",
	reloadButton: "Reload",
};

module.exports = {
	PKG_PROP_MAP,
	MESSAGES,
};
